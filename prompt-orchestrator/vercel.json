{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "ignoreCommand": "case \"$VERCEL_GIT_COMMIT_REF\" in claude/vertex-*|vertex/*) exit 0;; esac; git diff HEAD^ HEAD --quiet -- .",
  "env": {
    "NEXT_PUBLIC_DEMO_MODE": "true"
  }
}
